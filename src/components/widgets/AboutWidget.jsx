import React from 'react';
import {Box} from "@mui/material";

const AboutWidget = ({ onClose }) => {
    return (
        <Box>
            Work in progress...
        </Box>
    );
};

export default AboutWidget;